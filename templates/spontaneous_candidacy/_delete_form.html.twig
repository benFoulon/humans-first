<form method="post" action="{{ path('spontaneous_candidacy_delete', {'id': spontaneous_candidacy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spontaneous_candidacy.id) }}">
    <button class="btn">Delete</button>
</form>
